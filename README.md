# Voidwell Auth

### Database

#### Handling Migrations

Execute `dotnet ef migrations add <migration name>` to create a new migration.

### Docker

Certificates are stored in `/app/certificates`. Persist them with a volume mapping.